import Nav from "../Nav/nav";

const NotFound = () => {
  return (
    <div id="not-found-container" className="container">
      <Nav page="not-found" />
    </div>
  );
};

export default NotFound;
